defmodule PhoenixProdmin do
  @moduledoc """
  PhoenixProdmin is an Igniter installer that codemods a freshly-generated Phoenix
  project so it can boot under `MIX_ENV=prod` with no manual edits.

  See `Mix.Tasks.PhoenixProdmin.Install`.
  """
end
